const textButton = Object.freeze({
    main: 'Главная',
    settings: 'Настройки',
    item: 'Item'
});

const imgButton = Object.freeze({
    main: 'SidebarButtonIconGrey.svg',
    settings: 'settings.svg',
    item: 'item.svg'
});

const imgButtonActive = Object.freeze({
    main: 'SidebarButtonIcon.svg',
    settings: 'settingsActive.svg',
    item: '.itemActive.svg'
});

const pathButton = Object.freeze({
    main: '/',
    settings: '/settings',
    item: '/item',
})

export {textButton, imgButton, imgButtonActive, pathButton};